        - name: YII_DEBUG
          value: "0"
        - name: YII_ENV
          value: prod
        - name: APP_DATABASE
          valueFrom:
            secretKeyRef:
              name: {{ app_name }}-secret
              key: app_db_dsn
        - name: APP_LOG_LEVELS
          value: "{{ app_log_levels }}"
        - name: APP_LOG_TARGET
          value: "{{ app_log_target }}"
        - name: APP_GELF_HOST
          value: "{{ app_gelf_host }}"
        - name: APP_GELF_PORT
          value: "12201"
        - name: APP_GELF_TAGS
          value: "k8s-logs"
        - name: APP_GELF_LOGSTASH_PREFIX
          value: "{{ app_gelf_logstash_prefix }}"
        - name: APP_GELF_NAMESPACE
          value: "dream-team"
        - name: APP_REDIS_HOSTNAME
          value: "{{ app_redis_hostname }}"
